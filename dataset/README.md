# Dataset

This project expects authorized user images in the following structure:

```text
dataset/
└── faces/
    ├── Person1/
    │   ├── image1.jpg
    │   └── image2.jpg
    ├── Person2/
    │   └── image1.jpg
```

Each folder name is treated as the identity label.

Actual dataset images are not included for privacy reasons.
